modules:
  - type: dnf
    copr:
      - bieszczaders/kernel-cachyos
  - type: rpm-ostree
    install:
      - kernel-cachyos 
    remove:
      - kernel  # Stock mainline
      - kernel-core
      - kernel-modules
      - kernel-modules-extra
      - kernel-modules-core  # If present